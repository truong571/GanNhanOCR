__pycache__/
*.py[cod]
.venv/
.env
.DS_Store
.claude/
*.log

# Binary blobs — never commit (sync via HF Hub / Kaggle Datasets instead).
# push.sh has a runtime safety net that rejects these even if forced.
*.png
*.xlsx
*.pdf

# Pipeline outputs (regenerated by run_pipeline.sh, never commit)
prepared/
dataset/

# Universal FontDiffusion cache (pulled from HF Hub, ~1.5 GB)
prepared/_universal_fd_cache/
