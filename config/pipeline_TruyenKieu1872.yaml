# Config RIÊNG TruyenKieu1872 — bộ ĐỐI CHỨNG NGOÀI của IHR-NomDB (Kiều bản Duy Minh Thị 1872, mộc bản).
#
# ⚠️ ĐÂY LÀ TẬP ĐÁNH GIÁ, KHÔNG PHẢI TẬP HUẤN LUYỆN.
# Bộ này đã có NHÃN NGƯỜI từng chữ (data/TruyenKieu1872/manifest.tsv, cột nom_text) và QN câu-với-câu.
# Giá trị của việc chạy pipeline lên nó là ĐO ĐỘ ĐÚNG END-TO-END THẬT (so nhãn máy với nhãn người),
# KHÔNG phải để thêm dữ liệu huấn luyện. Bộ ra ở dataset/TruyenKieu1872/ phải được đánh dấu là tập đánh giá
# và KHÔNG trộn vào train/val của bất kỳ mô hình nào; adapter KHÔNG đọc nhãn Nôm gốc và KHÔNG ghi
# đè data/ (chỉ đọc). Đo: scripts/measure/ihr_endtoend_eval.py; báo cáo: docs/CHAY_3_BO_CON_LAI_2026-09-23.md.
#
# Bố cục đã đo (scripts/measure/ihr_layout.py → measure_out/TruyenKieu1872/ihr_layout/summary.json):
#   mỗi cột = ĐÚNG MỘT CẶP LỤC BÁT 6⧺8 = 14 chữ (1.529/1.530 cột có đủ 2 câu); 10 (159/162 trang) cột/trang; cột 1 = PHẢI nhất
#   (162/162 trang); px/chữ 40,0 (nhỏ hơn thạch bản ~4×) ⇒ cùng khuôn `layout: lithograph` của LVT1883/KVK1884.
books:
  - name: TruyenKieu1872
    # 2026-09-24 (PA3 tầng L3, docs/CHUA_QN_2026-09-24.md §4): sửa LỖI KÝ TỰ của tesseract trên âm
    # quốc ngữ NGOÀI từ điển, NGAY TRƯỚC align (pipeline/align_engine/qn_charfix.py; bảng ở
    # config/lexicon/qn_charfix.json: e→c, ð→đ, ñ→n, œ→c, ‹/›/¿→∅). Ba chốt: chỉ đụng âm ngoài từ
    # điển · chỉ nhận khi có ĐÚNG MỘT ứng viên là khoá từ điển · quyết định hoàn toàn ở phía quốc
    # ngữ (KHÔNG nhìn chữ kim) nên hai kênh vẫn độc lập. Chạy TRƯỚC tầng dời dấu có sẵn: đo được
    # ca `eó` (hỏng của `có`) bị tầng dấu dời thành `éo` — 要 ∈ R(éo) nhưng ∉ R(có) — tức một âm SAI
    # hợp thức hoá đúng chữ kim mà âm ĐÚNG sẽ loại. Mặc định TẮT (STT không đổi một byte).
    qn_charfix: true
    layout: lithograph
    n_columns: 10
    qn_syllables_per_column: 14
    # Ảnh gốc chỉ 40,0 px/chữ nhưng detector letterbox trang về 1024 px nên trong KHÔNG GIAN
    # DETECTOR chữ vẫn ~50 px — đúng bằng thạch bản (154 px/chữ trên trang 3.204 px). Vì vậy giữ
    # nguyên bộ tham số detector đã hiệu chuẩn cho thạch bản thay vì đặt lại từ đầu.
    det_xmargin: 0.05
    det_thr: 0.15
    box_decoder: pitch
    detector_ckpt: train_crop/detector_r34_v2_litho.pt
    # kim `lang_type = 2` (Nôm). ĐO TRÊN CHÍNH BỘ NÀY, có nhãn người nên là độ đúng THẬT, không
    # phải chỉ số thay thế: kim ∈ R(âm) 98,8 %, kim == chữ NGƯỜI GÁN 98,2 % ở ×1 (×3: 98,5 %) (10 trang, 100 cột).
    kim_lang_type: 2
    # DP chữ↔âm riêng từng tầng 6/8 (ranh giới câu lục/câu bát thành ràng buộc cứng).
    tier_dp: true
    detector_resize: area
    # 2026-09-23 (vòng 7): NGUỒN ĐIỂM ẢNH của crop GIAO NỘP. `processed` (mặc định, STT không đổi byte)
    # cắt từ prepared/<Book>/pages/*.png — ảnh adapter đã xám hoá + kéo nền (stretch/otsu) nên crop mất
    # nền giấy của bản quét. `original` cắt ĐÚNG CÙNG khung hình đó từ ảnh quét gốc trong data/<Book>/:
    # hình học (pad, seam carve, tighten_box) VẪN tính trên ảnh đã xử lý rồi áp nguyên xi, nên
    # crop_w/crop_h/ink_pct/seg_flag không đổi một chữ số; chỉ điểm ảnh (và image_md5) đổi. Bản đã xử lý
    # vẫn được ghi song song ở $out/crops_bin/ cho mã đọc crop nhị phân (enrich_crop_quality tự ưu tiên
    # thư mục này nên crop_quality_flag/stray_ink/border_ink giữ nguyên). crops_bin/ KHÔNG giao nộp.
    crop_source: original
    reocr: false

# Khối `run:` CHỈ run_pipeline.sh --book đọc.
run:
  ingest: ihr                   # pipeline.tools.ingest_ihr_book (bboxes.json + annotation.json + kim)
  # --scale 1: đã đo kim ×1 vs ×3 trên chính bộ này, chênh lệch trong nhiễu ⇒ không phóng ảnh
  # (×3 chỉ làm crop xuất ra to hơn, không đổi độ đúng nhãn). --qn-count-rule: luật 6/8.
  ingest_args: "--qn-count-rule"
  dataset_out: prepared/TruyenKieu1872/dataset_out   # giữ mọi thứ dưới prepared/, không tạo thư mục ở gốc repo
  n_columns: 10
  cross: false                  # KHÔNG có dị bản số hoá riêng; độ đúng đo bằng NHÃN NGƯỜI (ihr_endtoend_eval)
  measure_steps: ihr_layout

paths:
  data_dir: prepared
  output_dir: dataset/TruyenKieu1872
  dict_dir: Dict
  qn_to_nom_dict: Dict/QuocNgu_SinoNom.csv
  similar_dict: Dict/SinoNom_Similar.csv
  font_path: font_diffusion/fonts/NomNaTong-Regular.ttf
  # Production weights from HF root (dzungpham/font-diffusion-weights). The
  # /FST/checkpoint_step_1500/ subfolder is an undertrained snapshot — never use it.
  # Both keys point at the same dir; phase1 is unused while use_fst=False in
  # core/ranking/fontdiffusion_gen.py but kept for forward compatibility.
  fontdiffusion_ckpt: font_diffusion/ckpt/PROD
  fontdiffusion_phase1_ckpt: font_diffusion/ckpt/PROD
  # Universal FontDiffusion cache (~89k glyph U+*.png) — data ĐÃ SINH SẴN ở
  # local gannhanocr-fd/ (thư mục hex). step3 nạp ĐỆ QUY (rglob "U+*.png"),
  # TRA theo Unicode codepoint, KHÔNG sinh mới (skip_local_fd_gen: true).
  fd_cache_universal: gannhanocr-fd

step1:
  dpi: 300
  denoise: true
  crop_size: 64
  sauvola_k: 0.2
  sauvola_window: 25
  min_stroke: 2
  use_ocr_api: true
  # QN re-OCR line detector (replaces the old raw horizontal projection that
  # broke on skewed scans). Options:
  #   auto             - DBNet (PaddleOCR) if installed, else projection_deskew
  #   dbnet            - PaddleOCR DB detector (handles skew, warps lines flat)
  #   projection_deskew- estimate page skew, deskew, then projection (no extra dep)
  #   projection       - original raw projection profile (legacy / A-B baseline)
  # 2026-07-21: GHIM CỨNG, KHÔNG để `auto`.
  # `auto` = dbnet nếu máy có paddleocr, ngược lại projection_deskew
  # (core/ocr/line_detector.py:54). Môi trường sinh ra số liệu luận văn là
  # py3.14 KHÔNG có paddleocr -> đã chạy projection_deskew. Nếu để `auto`,
  # một máy khác có paddleocr sẽ tách dòng bằng backend KHÁC mà không báo gì,
  # cho ra số liệu lệch âm thầm.
  qn_line_detector: projection_deskew

step2:
  deletion_cost_small: 0.3
  deletion_cost_medium: 0.6
  deletion_cost_normal: 1.2
  insertion_cost: 1.0
  # --- HÌNH HỌC CẮT ẢNH ------------------------------------------------------
  # CẢ HAI NÚM DƯỚI ĐÂY ĐỀU ĐƯỢC build_dataset.py ĐỌC THẬT (nối 2026-08-25).
  # Trước đó `crop_pad_frac: 0.18` là DÒNG CHẾT: không mã nào đọc, `--pad` mặc định
  # 0.12, và run_pipeline không truyền — nên 66k crop đã giao thực ra cắt ở 0,12
  # trong khi cấu hình khai 0,18. Nay giá trị ở đây LÀ giá trị chạy.

  # Nới mỗi cạnh hộp thêm phần này của (w, h) TRƯỚC khi cắt, để crop bắt trọn nét
  # (đuôi chữ, dấu trên, bộ bên). Đo T4-B': ở 0 thì 51,7% crop bị cắt mất nét; ở
  # 0,12 chỉ còn 0,4%; lên 0,45 chỉ bớt thêm 0,2 điểm mà đẩy dính-láng-giềng từ
  # 51% lên 69% số crop. Nên 0,12 là điểm làm việc, đã đo chứ không phải mặc định.
  crop_pad_frac: 0.12

  # Biên nới DỌC của hộp ký tự, theo BƯỚC LẶP của cột: hộp cao = pitch × (1 + 2F).
  # Xem align_production.BOX_OVERLAP_FRAC. Đổi giá trị này = CẮT LẠI TOÀN BỘ CROP.
  #
  # 🔴 ĐÃ THỬ 0 VÀ HOÀN NGUYÊN (2026-08-25). Lý do đầy đủ ở BOX_OVERLAP_FRAC.
  box_overlap_frac: 0.10

  # A-6 (flow N3f, 2026-09-16): hộp thô detector cho luật --box-rule syl_index.
  # Ngưỡng tin cậy CenterNet và biên x lọc hộp theo cột (± phần của bề rộng cột).
  # Đo trên cột OCR=QN: 0,2 / ±0,25w cho M==N 93,2% (cũ 0,3 / ±0,5w: 69,9%).
  # `--box-rule legacy` KHÔNG đọc hai khoá này (trọn gói 0,3 / ±0,5w để tái lập bộ cũ).
  det_thr: 0.2
  det_xmargin: 0.25

# Step 3 (legacy DINOv2 labeling path) — RETIRED: superseded by pipeline/align_engine Step 2
# (build_dataset.py --use-s3, S3 = trained Nôm encoder). DINOv2 is DISABLED here
# (proven non-discriminative on Nôm — pipeline/align_engine/README.md).
# Code kept (core/ranking/dinov2_ranker.py), NOT deleted, for the thesis comparison.
step3:
  use_dinov2: false                 # DISABLED — replaced by trained Nôm embedder (S3)
  dinov2_model: dinov2_vitb14_reg   # Base + registers — best for fine-grained chu-Nom
  dinov2_threshold: 0.75            # cosine similarity threshold for matched=True
  classical_threshold: 0.55         # classical CV fallback (Hu+IoU+projection)
  use_fontdiffusion: true
  require_fontdiffusion: true       # if true: tier 3 ONLY uses fd_cache (no font-rendered fallback)
  # When true, step 3 will NOT try to generate missing FontDiffusion images
  # locally (~7s/img on Mac CPU = bottleneck). Chars missing from fd_cache get
  # no Tier-3 candidate — they fall through to Tier 2 or unmatched.
  # Offload generation to Kaggle via lab/kaggle_diffusion/diffusion_run.ipynb.
  skip_local_fd_gen: true

step4:
  min_samples_per_class: 1
