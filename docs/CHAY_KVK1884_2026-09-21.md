# Chạy end-to-end Kim Vân Kiều 1884 (thạch bản, 163 trang, có kim) — 2026-09-21/22

> **Ghi chú 22/09:** `docs/KVK1884_TRANG_CAN_XAC_NHAN.csv` (27 trang formula≠anchor) đã **LỖI THỜI** sau khi ingest bằng `--verse-map content`; danh sách người cần xem thay bằng **9 cột không khớp** ở §2 (126 ô REVIEW).

Tài liệu này **thay** `docs/CHAY_KVK1884_THU_2026-09-21.md` (thử 8 trang, `--verse-map anchor`, det_xmargin ±0,25w / thr 0,2);
số 8 trang cũ được giữ ở cột "THU 8 trang" của các bảng để đối chiếu. Lượt này (NV-D, 22/09 08:53 → 09:07) chạy trọn
B0→B5 cho 163/163 trang, không bước nào lỗi, **không sửa mã engine**; chỉ đổi 1 dòng config (`det_thr: 0.15`).
Lúc 09:07 không còn tiến trình pipeline nào chạy. **Chưa commit.**

## 0. Vì sao phải ingest lại: `--verse-map anchor` ghép sai QN từ trang 54

Lượt trước (NV-C, 00:30–00:58) đã ingest 163 trang bằng `anchor` (trang 163 bị bỏ vì verses.tsv "thiếu" 3254–3256) và build
162 trang → **GOLD chỉ 5.108 / 22.558 ô (22,6 %), REVIEW 73 %**. Chẩn đoán theo NỘI DUNG (`diag_content.py`: mỗi cột Nôm
(chữ kim) chấm điểm với từng cặp dòng QN liên tiếp = số chữ tra được âm trong `Dict/QuocNgu_SinoNom.csv`, DP đơn điệu theo cột)
cho thấy từ **trang 54 cột 7** trở đi cặp dòng QN đúng nằm **sớm hơn 4 dòng** so với số câu (điểm cột đang gán ≈ 5/140 chữ,
cột đúng ≈ 90/140) — tức là **số câu in trong bản Nôm = seq QN + 4** (rồi +5 từ trang 153): verses.tsv thiếu 4 dòng vật lý
quanh câu 1069–1072 và 1 dòng quanh 3045–3050. `anchor` neo theo số in nên kéo cả 109 trang lệch. Lượt trước đã thêm
`--verse-map content` vào adapter (`content_rows_for_page`, `_placeholder_row`; cột không khớp → 14 token `khongkhop`
→ `is_plausible_qn_syllable()=False` → chỉ có thể REVIEW) và thử 5 trang (53, 54, 55, 153, 163: GOLD 397/669); manifest lúc
đó chỉ còn 5 trang. Lượt này ingest lại **toàn bộ 163 trang bằng `content`** (0 gọi API, đọc `kim_raw/`).

Hệ quả: (a) "trang 163 thiếu 3 dòng QN" là ảo — QN có 3.251 dòng, Nôm đánh số tới 3.256, lệch 5 = 4 dòng mất ở 1069–1072 +
1 dòng ở ~3045; trang 163 (8 cặp) ghép đủ 8 cột với offset −5 (seq 3236–3251, câu cuối "Mua vui cũng được một vài trống canh");
(b) "vùng 2220–2240" và 27 trang formula≠anchor (`docs/KVK1884_TRANG_CAN_XAC_NHAN.csv`) **không cần người xác nhận offset nữa** —
`content` quyết từng cột bằng chữ; việc người cần xem chuyển thành 9 cột không khớp (§2).

## 1. Lệnh đã chạy (CLI thật — FACTS)

| # | Lệnh | Kết quả | Thời gian |
|---|---|---|---|
| 1 | `ingest_lithograph_book --book KimVanKieu1884 --ocr kim --contrast otsu --verse-map content --out prepared` | 163/163 trang, `ocr_calls` **0** (đọc kim_raw, hash khớp), `n_cache_ok` 163/163 | 08:53:59 → 08:54:02 (3 s) |
| 2a | `diag_thr_kvk.py` (quét det_thr NATIVE 0,3/0,2/0,15/0,1, biên x 0,05, 1.630 cột/163 trang) | chọn **det_thr 0,15** (§3) | 4 ph 16 s |
| 2b | `build_dataset --config <cfg det_thr 0,2 \| 0,15> --reseg detector --qd01-cells none --decisions none --use-s3 --two-pass --box-rule syl_index --force --limit 8` + `remediation census` | bảng §3 | ~1 ph |
| 2c | `config/pipeline_KimVanKieu1884.yaml`: thêm `det_thr: 0.15` (+ lý do) ; `step0_setup` | Validation passed; `BookLayout(lithograph,10,14, det_xmargin 0.05, det_thr 0.15)` | 2 s |
| 3 | `build_dataset --config config/pipeline_KimVanKieu1884.yaml --reseg detector --qd01-cells none --decisions none --use-s3 --two-pass --box-rule syl_index --force --out dataset_out_KimVanKieu1884` | 22.704 ô: GOLD 15.133 / SILVER 0 / SYLLABLE 3.520 / REVIEW 4.051; log `detector theo sách thr = 0.15 \| biên x = ±0.05w` | 09:03:14 → 09:06:25 (**3 ph 11 s**) |
| 3b | `enrich_crop_quality --labels … --src-root …` + dòng `build` vào `CHECKSUMS.txt` (sha256 e7d7fbb2…) | 45 cột; ok 16.432 / bleed 1.993 / **blank 216 (1,16 %)** / truncated 12 | 5 s |
| 4 | `remediation census` / `apply --tau 0.62` | AE-1 0; **F1 cross-col 90 hàng / 45 nhóm**; MD5_DUP 0; quarantined **88** (conflict 86, dup 2); demoted similar-bridge 0; split-leak 0 | 3 s |
| 4b | `confusion_fix --in labels_remediated.csv --out labels_final.csv --fixes config/confusion_fixes.yaml --measure` | n_fixes 1, **total_demoted 5 ≠ 0** (§4); precision GOLD = null (chưa có verdict người — đúng) | 2 s |
| 5 | `export_final_dataset.py --labels labels_final.csv --src-root dataset_out_KimVanKieu1884 --out dataset_KimVanKieu1884` | **18.560 ảnh copy, 0 thiếu**; 226 ô blank/truncated ghi `labels_trace.csv`; 0 dòng "không đủ 9 cột" | 3 s |
| 5b | `make_dataset_docs --dataset dataset_KimVanKieu1884`; `make_xlsx --labels dataset_KimVanKieu1884/labels.csv` | README/DATASHEET/NGUON_THU_TICH/LICENSE + labels.xlsx (18.560 × 12); 7 mục ⬜ chờ người giữ bản quét | 2 s |

KHÔNG chạy: `self_training_rescue` (theo yêu cầu), `update_bang_so_lieu`, `check_ocr_columns`. Trước bước 3 đã dời
`dataset_out_KimVanKieu1884/` cũ (build 162 trang anchor, 48 MB) và xoá khung rỗng `dataset_KimVanKieu1884/`.
Log/tệp trung gian (thư mục tạm của phiên, không bền): `/private/tmp/claude-501/-Users-truongmdn-TruongMDN-ThS-DoAn-GanNhanOCR/0d0d06b0-3982-4e6b-8b3f-343a997468d0/scratchpad/nvD/`
(`ingest_163c.log`, `diag_thr_kvk.py`, `diag_thr_163.csv`, `cfg_thr0{20,15}.yaml`, `build8_thr0{20,15}/`, `census8_*.log`, `build163.log`,
`enrich.log`, `census.log`, `apply.log`, `cfix.log`, `export.log`, `metrics_kvk.py`, `dataset_out_KimVanKieu1884_anchor162/`, `manifest_content5_backup.json`).

## 2. Cổng ingest (`prepared/KimVanKieu1884/manifest.json`, 163 trang, `verse_map: content`)

| Cổng | 163 trang (content) | THU 8 trang (anchor) |
|---|---|---|
| n_pages / n_pages_10cols | 163 / **162** (page_0163 = 8 cặp, đúng layout_pages.csv) | 8 / 8 |
| n_cols | 1.628 (= 3.256 câu Nôm) | 80 |
| n_cols_syll14 (QN đủ 14 âm) | **1.511 / 1.628 (92,8 %)** — 117 cột lệch do OCR QN, chỉ cờ | 67/80 (83,8 %) |
| n_cols_kim14 = n_cols_kim_6_8 | **1.560 / 1.628 (95,8 %)** — 68 cột kim lệch (6+7, 7+8…) giữ hộp thật + cờ | 77/80 (96,3 %) |
| n_boxes_unassigned / n_number_boxes lọc / n_chars_nonpair | 27 / 210 / 6 | 2 / 15 / 0 |
| n_cache_ok (`verify_cache_image`) / ocr_calls | **163 / 163** / **0** | 8/8 / 8 |
| content: offset cột so với công thức | 0: 534 cột (trang 1 → 54 c4) · **−4: 983** (54 c7 → 153 c4) · **−5: 102** (153 c6 → 163) · không khớp: **9 cột** | (anchor: differs 0/8) |
| content: điểm khớp (chữ tra được âm / 140 chữ) | trung vị 88, min 69 (trang 163, 112 chữ) ≈ 63 % chữ; 0 trang `content_lowscore` | — |
| cờ theo loại | page_flag kế thừa verses.tsv 398, verse_map 171, n_syll 126, kim 68, line_flag 19, n_pairs 1 | 47 / 16 / 3 |

**9 cột không khớp nội dung** (→ 126 ô `khongkhop`, 100 % REVIEW, không vào bộ export) — đây là danh sách người cần xem thay cho 27 trang cũ:

| Trang | Cột | Câu Nôm (số in) | Diễn giải |
|---|---|---|---|
| 54 | 5, 6 | 1069–1072 | **4 dòng QN không có trong verses.tsv** (cột trước/sau khớp offset 0 → −4): tesseract mất khối hoặc bản QN bỏ; từ đây số in Nôm = seq QN + 4 |
| 153 | 5 | 3049–3050 | quanh chỗ mất 1 dòng QN (seq ~3045, "Đã tu tu trót…") → offset −4 → −5 |
| 76 c10 · 80 c7 · 143 c7 · 145 c6 · 150 c2 · 158 c6 | 1 cột mỗi trang | 1519–1520 · 1593–1594 · 2853–2854 · 2891–2892 · 2983–2984 · 3151–3152 | offset hai bên **không đổi** (−4/−5) → dòng QN có nhưng điểm < 4 chữ (OCR QN rác hoặc chữ Nôm ngoài từ điển); người đối chiếu 2 dòng QN tương ứng |

## 3. Chọn `det_thr` cho KVK (biên x 0,05 đã khoá mặc định lithograph)

Quét NATIVE 4 ngưỡng (mỗi ngưỡng một detector, không lọc từ superset) trên **1.630 cột / 163 trang** (`diag_thr_163.csv`; w/pitch trung vị 0,97, pitch 126 px):

| det_thr | n_det == N | trong 1.511 cột n_qn = 14: n_det − 14 ∈ {−3, −2, −1, **0**, +1, +2} |
|---|---|---|
| 0,3 | 44 / 1.630 = **2,7 %** | 209 / 160 / 109 / **38** / 2 / 0 (mất hộp hàng loạt: nét KVK mảnh, điểm tin cậy thấp) |
| 0,2 (toàn cục) | 742 = **45,5 %** | 70 / 162 / 332 / **728** / 109 / 9 (lệch chủ yếu −1/−2 = MẤT hộp) |
| **0,15 (chọn)** | 965 = **59,2 %** | 28 / 48 / 188 / **950** / 242 / 36 (cân −1/+1) |
| 0,1 | 962 = 59,0 % | 3 / 8 / 42 / **942** / 415 / 85 (thừa hộp +1) |

Build `--limit 8` (80 cột, 1.116 ô) đối chiếu 0,2 vs 0,15 (cùng biên x 0,05):

| det_xmargin / det_thr | n_det==N | F1 cross-col | box_source detector/midpoint/split | count_source conflict | Tier |
|---|---|---|---|---|---|
| 0,25 / 0,2 (THU 21/09) | 51/80 = 63,8 % | 22 / 11 | 975 / 122 / 19 | 336 | GOLD 744 / SYL 38 / REVIEW 334 |
| 0,05 / 0,2 | 47/80 = 58,8 % | **0 / 0** | 955 / 139 / 22 | 392 | như trên (không đổi) |
| **0,05 / 0,15** | **53/80 = 66,2 %** | **0 / 0** | 1.044 / **62** / **10** | **294** | như trên (không đổi) |

Lý do chọn 0,15: n_det==N cao nhất trong quét toàn sách (0,1 chỉ tương đương nhưng thừa hộp +1 gấp đôi), giảm midpoint/split/conflict
trên 8 trang, tier không đổi (tier là luật văn bản); trùng giá trị LVT1883 nên hai sách thạch bản dùng chung một cặp tham số. Ghi vào
`config/pipeline_KimVanKieu1884.yaml` (`books[0].det_thr: 0.15`, kèm số đo). Chưa quét 0,12–0,13 (đỉnh có thể nằm giữa 0,1 và 0,15 nhưng
chênh < 1 điểm).

## 4. Kết quả build 163 trang (`dataset_out_KimVanKieu1884/`)

| Chỉ số | KVK1884 163 trang | Cổng | Đạt? | THU 8 trang | LVT1883 (NV-B) |
|---|---|---|---|---|---|
| page_ok (`summary.layout_gate`) | **162 / 163** (hybrid_9 162); không ok: page_0163 (n_qn_cols 8 ≠ 10, `projection_fallback`) — nhưng 8 cột dùng đúng 8 cột kim (n_ocr 14), nội dung đúng (c1: 風流富貴埃皮 = "phong lưu phú quí ai bì", câu 3241), GOLD 72/109 | ≥ ~98 % | ✅ 99,4 % | 8/8 | 103/105 |
| Cột (page, column) | 1.628; `column` ∈ 1…10; n_qn = 14: 1.511; n_qn ∈ 14±2: 1.627 (99,9 %) | | ✅ | 80 | 1.041 |
| Tổng ô | 22.704 (1.862 lớp chữ) | | | 1.116 | 14.476 |
| Tier thô GOLD / SILVER / SYLLABLE / REVIEW | **15.133 (66,7 %) / 0 / 3.520 (15,5 %) / 4.051 (17,8 %)** | chế độ A có GOLD, SILVER 0 | ✅ | 744 / 0 / 38 / 334 | 9.680 / 0 / 1.685 / 3.111 |
| tier_v3 | CHAR_A 14.487 / CHAR_B 646 / SYL 3.520 / REVIEW 4.051 | | | | |
| **M == N** (n_ocr == n_qn) | **1.445 / 1.628 = 88,8 %** | | ✅ (cao hơn LVT) | 80,0 % | 83,8 % |
| **n_det == N** (I5) | **964 / 1.628 = 59,2 %** (trong cột n_qn=14: 950/1.511 = 62,9 %); n_det == M 990 | **≥ 75 %** | ❌ **không đạt** | 63,8 % (xmargin 0,25) | 65,2 % |
| box_source detector / midpoint / split | 19.436 / 3.061 / 207 | | | 975/122/19 | 13.199/1.247/30 |
| count_source equal_qn / equal_ocr / conflict | 13.466 / 1.088 / 8.150 | | | | |
| ocr_char (kim) | **100 %** (22.704/22.704) | 100 % | ✅ | 100 % | 100 % |
| p_register ≥ 0,8 | 22.628 (99,7 %); band_touched 0 | | ✅ | 98,9 % | 99,0 % |
| n_anchor_pairs | 4.244 (26/trang) | | ℹ️ | 25 | 2.292 |
| Luật | s1_inter_s2_direct 14.362; no_context 3.648; syl_ctx:corpus4 2.098; bigram 926; similar 645; tone 496; not_plausible 402; direct_am_sua_dau 125 | | | | |
| QĐ-01 | locked 0, pending 0 | 0 | ✅ | 0 | 0 |
| REVIEW theo trang | trung vị 17,9 %, cao nhất 29 % (page_0076); **0 trang > 50 %** | | ✅ | ≤ 37 % | 2 trang biên |
| Remediation | F1 cross-col **90 ô / 45 nhóm** → quarantine **88** (conflict 86, dup 2); demoted 0; AE-1 0 | | ✅ | 22/11 (chưa apply) | 16/8 → 16 |
| confusion_fix | n_fixes 1, **total_demoted 5**: 5 ô GOLD `syllable=người`, label 㝵 (U+3775; ocr_char kim 旱 U+65F1, luật s1_inter_s2_similar) → REVIEW `confusion_fix:systematic_confusion_nguoi_3775` — đúng luật `config/confusion_fixes.yaml` (LVT = 0 vì không có ô 㝵/người GOLD) | demoted == 0 kỳ vọng SPEC §6 | ⚠️ 5 ≠ 0, có lý do | — | 0 |
| labels_final GOLD / SYLLABLE / REVIEW / QUARANTINE | **15.056 / 3.504 / 4.056 / 88** | | | | 9.666 / 1.683 / 3.111 / 16 |
| Export `dataset_KimVanKieu1884/` | **18.560 ảnh (gold 15.056 + syllable 3.504), 0 thiếu**; labels.csv 18.560 dòng, labels_trace.csv, columns.csv, labels.xlsx, README/DATASHEET/NGUON_THU_TICH/LICENSE | 0 thiếu | ✅ | chưa export | 11.349 |
| Crop chất lượng | blank **216 (1,16 %)** (GOLD 186, SYLLABLE 30), truncated 12; 226 ô trong bộ export có cờ ở labels_trace.csv | | ⚠️ | — | blank 6 |
| `summary.json` | `detector_params_by_book: {KimVanKieu1884: {det_thr 0.15, det_xmargin 0.05}}`; `layout_gate` như trên | | | | |

Về **blank 216** (LVT chỉ 6): đo 40 ô blank ngẫu nhiên — tỉ lệ mực trong hộp ~4,7 % trên ảnh otsu và ~5,1 % trên **ảnh quét gốc**
(ô ok: 9,7 % / 10,5 %; 40/40 ô có mực > 2 % trên ảnh gốc) → **không phải otsu xoá nét**; là chữ nét mảnh/ít nét (hộp 112×141 px) rơi dưới
ngưỡng `enrich_crop_quality` hiệu chuẩn trên STT. Chỉ gắn cờ, không loại; người kiểm nên xem qua `labels_trace.csv` (crop_quality_flag).

## 5. 10 ô GOLD mẫu để người kiểm (`labels_final.csv`, `random_state=20260921`)

| image | page | col | nom_idx | ocr_char = label | syllable |
|---|---|---|---|---|---|
| gold/kimvankieu1884_page_0042_c08_099.png | page_0042 | 8 | 1 | 塵 | trần |
| gold/kimvankieu1884_page_0069_c08_106.png | page_0069 | 8 | 8 | 柴 | thầy |
| gold/kimvankieu1884_page_0074_c04_044.png | page_0074 | 4 | 3 | 所 | sửa |
| gold/kimvankieu1884_page_0074_c10_137.png | page_0074 | 10 | 12 | 苔 | đầy |
| gold/kimvankieu1884_page_0075_c06_073.png | page_0075 | 6 | 4 | 陰 | âm |
| gold/kimvankieu1884_page_0080_c05_063.png | page_0080 | 5 | 7 | 泇 | dơ |
| gold/kimvankieu1884_page_0091_c03_036.png | page_0091 | 3 | 8 | 傳 | truyền |
| gold/kimvankieu1884_page_0103_c04_046.png | page_0103 | 4 | 4 | 典 | đến |
| gold/kimvankieu1884_page_0123_c08_104.png | page_0123 | 8 | 6 | 便 | tiện |
| gold/kimvankieu1884_page_0127_c09_117.png | page_0127 | 9 | 6 | 娘 | nàng |

(tất cả `s1_inter_s2_direct`, p_register 1,0; 10/10 tệp có trong `dataset_KimVanKieu1884/gold/`; `column` phải→trái 1…10;
`nom_idx` 0–5 tầng trên (câu lục), 6–13 tầng dưới (câu bát). 10 ô THU 8 trang cũ (page 2–8) vẫn cùng nhãn nhưng tên tệp/md5 có thể đổi do hộp đổi.)

## 6. Xử lý trang/cột có vấn đề dữ liệu (chỉ cờ, không sửa engine)

| Vấn đề | Xử lý | Bằng chứng |
|---|---|---|
| Trang 163 "thiếu 3 dòng QN" | KHÔNG bỏ trang: `content` ghép đủ 8 cột (offset −5, seq 3236–3251); cờ `n_pairs=8!=10`; layout_gate page_ok=False do n_qn_cols 8 ≠ 10 | GOLD 72/109, REVIEW 16 %; c1 = câu 3241 đúng |
| Vùng 2220–2240 (neo 2230 +2 / 2240 +1 mâu thuẫn) | không còn là vấn đề: content offset −4 liên tục 55 → 153 c4; trang 107–117 điểm khớp 75–99/140 | manifest `verse_map.content.offset` |
| 4 dòng QN mất (1069–1072) + 1 dòng (~3045) | 3 cột placeholder `khongkhop` → REVIEW 42 ô; các cột sau tự dịch offset | §2 |
| 6 cột lẻ không khớp (76 c10, 80 c7, 143 c7, 145 c6, 150 c2, 158 c6) | placeholder → REVIEW 84 ô; người đối chiếu dòng QN | §2 |
| 117 cột QN không đủ 14 âm | cờ `n_syll`, không ép; kéo M≠N 183 cột | manifest |
| 5 ô người/㝵 | confusion_fix → REVIEW (luật có sẵn) | §4 |
| 216 ô blank / 12 truncated | cờ `crop_quality_flag` ở labels_trace.csv, vẫn export | §4 |

## 7. Việc còn mở

1. **I5 không đạt** (59,2 % < 75 %) như LVT; det_thr đã tối ưu (đỉnh 0,15). Gốc còn lại: detector CenterNet (học STT) đếm lệch ±1 trên
   thạch bản (−1: 188, +1: 242 cột) + 117 cột QN lệch số âm. Chưa có chặn `n_det ≠ N → REVIEW`. Cần NMS theo bước dọc hoặc fine-tune detector.
2. **Người kiểm ≥ 300 chữ GOLD** KVK (10 ô §5) + 9 cột không khớp (§2) + 226 ô blank/truncated — chưa có ground truth người; chưa công bố tier GOLD.
3. `--verse-map content` chưa có selftest riêng (`ingest_lithograph_selftest` vẫn 35/35 = bộ cũ); `CONTENT_WINDOW 60 / SKIP_PEN 2 / MIN_COL 4` là hằng cứng;
   nên đối chiếu thêm với bản phiên âm chuẩn (Nôm Foundation) khi có — cũng chữa 117 cột QN lệch số âm.
4. `docs/KVK1884_TRANG_CAN_XAC_NHAN.csv` (27 trang formula≠anchor) đã lỗi thời sau `content`; danh sách mới = 9 cột ở §2.
5. `--use-s3` vẫn dựng 0 lớp nguyên mẫu crop (index.csv trỏ `dataset_out/` STT đã xoá — lỗi sẵn có), ~6.500 dòng WARN imread trong build log; tier không phụ thuộc S3 ở đường `--two-pass`.
6. `make_dataset_docs` sinh README/DATASHEET theo khuôn STT; NGUON_THU_TICH.md cần người rà cho KVK1884 (7 mục ⬜).
7. Chưa commit; không đụng `data/`, `prepared/SachThanhTruyen*`, `prepared/LucVanTien1883`, `dataset_out/`. Diff Guest Mode `core/ocr/ocr_api.py` không đụng (lượt này 0 gọi API).
8. Hồi quy STT 3 trang chưa chạy lại trong lượt này (không sửa mã engine/adapter; chỉ config KVK) — chạy lại trước khi commit theo `HUONG_DAN` §3.
