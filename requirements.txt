# Core pipeline (required)
numpy
opencv-python
scipy
PyYAML
requests
Pillow
PyMuPDF
openpyxl

# Step 3 visual ranking (DINOv2)
torch
torchvision
transformers
huggingface-hub
safetensors

# FontDiffusion (mandatory for tier-3 visual ranking)
diffusers
accelerate
einops
kornia
info-nce-pytorch
fonttools
pygame
scikit-image
datasets

# Quoc Ngu re-OCR for Sach* books (Step 1, when reocr=true). Lazy-imported.
# vietocr's pip metadata pins old Pillow/prefetch_generator; install with --no-deps
# then add runtime deps explicitly:
#   pip install --no-deps vietocr
#   pip install gdown prefetch_generator pyyaml lmdb einops
vietocr
gdown
prefetch_generator
lmdb
einops
